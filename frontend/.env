# === Frontend Environment Variables ===
# 注意：在 Vite 项目中，环境变量必须以 VITE_ 开头才能在客户端代码中访问

# API Base URL - 后端服务地址
# 使用相对路径 /api，通过 Vite 代理转发，支持局域网访问
VITE_API_URL=/api

# 注意：DeepSeek API Key 已移至后端 .env 文件中，前端不再需要配置
